apiVersion: v1
kind: Service

metadata:
  name: origoss-service
  namespace: origoss

spec:
  selector:
    app: origoss-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: ClusterIP